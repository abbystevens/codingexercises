/* 
Take a string of text.
Slice it based on the spaces.
Each value becomes part of an array.
Cycle through the array, if the item is already there, count +1. 
At the end sort the array based on the number of items, return the highest number.
*/

var string = "";
var space = " ";
var count = 0;

var array = string.split(space);

array.forEach(function(item)) {
	if(item === item) {
		count +=;
	} else {
		count 
	}
}

string

